services:
  - type: web
    name: wattwise
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn WattWise-AI.src.api:app -w 1 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11